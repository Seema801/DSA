#include <iostream>
#include <algorithm>

int main() {
    int arr[] = {1, 3, 5, 7, 9};
    int key = 5;
    if (std::binary_search(std::begin(arr), std::end(arr), key))
        std::cout << "Element found\n";
    else
        std::cout << "Element not found\n";
    return 0;
}
